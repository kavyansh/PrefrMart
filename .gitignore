node_modules/
.next/
out/
build/
.DS_Store
*.pem

# env
.env
.env*.local
!.env.example

# database (regenerate with `npm run db:reset`)
*.db
*.db-journal
*.db-shm
*.db-wal

# generated Prisma client (~10MB, regenerated by postinstall / `npm run db:generate`)
src/generated/

# Generated seed art is committed deliberately. It is deterministic, 192K in total, and the
# products reference it by key — leaving it ignored meant every product image 404d on Vercel,
# which clones from git and never runs `npm run gen:images`. Regenerate with that script.

# test / tooling output
coverage/
playwright-report/
test-results/
tsconfig.tsbuildinfo
next-env.d.ts
